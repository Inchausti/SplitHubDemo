{
  "version": "0.0.1",
  "configurations": [
    {
      "name": "SplitHub Dashboard",
      "runtimeExecutable": "python",
      "runtimeArgs": ["-c", "import os, http.server, functools; port=int(os.environ.get('PORT',9000)); http.server.test(HandlerClass=functools.partial(http.server.SimpleHTTPRequestHandler, directory='splithub-docs'), port=port, bind='')"],
      "port": 9000,
      "autoPort": true
    }
  ]
}
